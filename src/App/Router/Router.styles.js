export default (theme) => ({
  container: {
    paddingTop: theme.headerHeight,
    marginLeft: 12,
    marginRight: 12,
  },
  content: {
    minHeight: 'calc(100vh - 160px)',
    paddingBottom: 30,
    margin: '0 auto',
    maxWidth: 1024,
  },

})
